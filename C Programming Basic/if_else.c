#include <stdio.h>

int main()
{
    int num = 5;
    if(num >= 0)
    {
        printf("The number is positive");
    }
    else
    {
        printf("The number is not positive");
    }

    return 0;
}